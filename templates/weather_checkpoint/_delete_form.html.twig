<form method="post" action="{{ path('weather_checkpoint_delete', {'id': weather_checkpoint.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ weather_checkpoint.id) }}">
    <button class="btn">Delete</button>
</form>
